# Contributing

Small guidance for working on this repository:

- Create a feature branch: `git checkout -b feat/your-feature`
- Run tests locally: `make install` then `make test`
- Run linters: `make lint`
- Use pre-commit hooks: `pre-commit install`
- Commit messages: be concise and explain "what" and "why".
