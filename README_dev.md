# Developer Notes

Developer checklist and commands for setting up a reproducible development environment.

1. Create a venv and install dependencies:

```bash
make install
```

2. Run tests locally:

```bash
make test
```

3. Enable pre-commit hooks:

```bash
pip install pre-commit
pre-commit install
```
